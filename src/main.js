import { createApp } from 'vue'
import App from './App.vue'
import Bootstrap from 'bootstrap'

import 'bootstrap/dist/css/bootstrap.min.css'

createApp(App).mount('#app')
createApp(App).mount('#slider')

// VueElement.use(Bootstrap)

